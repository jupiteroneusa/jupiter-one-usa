const fs = require('fs');
let a = fs.readFileSync('admin/index.js', 'utf8');
const nl = a.includes('\r\n') ? '\r\n' : '\n';

// 1. Add beforeunload warning + Save Draft button to the review page
// Find the Send Quote button and Cancel link at bottom of review page
const oldButtons = `'<div style="display:flex;gap:10px;"><button type="submit" class="btn btn-gold" style="padding:12px 28px;">Send Quote to Customer &rarr;</button>' +${nl}      '<a href="/admin/rfqs/'+rfq.id+'" class="btn btn-outline" style="padding:12px 20px;">Cancel</a></div></form>' +`;
const newButtons = `'<div style="display:flex;gap:10px;">' +${nl}      '<button type="submit" class="btn btn-gold" style="padding:12px 28px;">Send Quote to Customer &rarr;</button>' +${nl}      '<button type="button" class="btn btn-outline" style="padding:12px 20px;border-color:#4caf50;color:#4caf50;" onclick="saveDraft()">Save Draft</button>' +${nl}      '<a href="/admin/rfqs/'+rfq.id+'" class="btn btn-outline" style="padding:12px 20px;" id="back-btn">Back to RFQ</a>' +${nl}      '</div></form>' +`;
if (a.includes(oldButtons)) { a = a.replace(oldButtons, newButtons); console.log('Draft button: ADDED'); }
else console.log('Draft button: NOT FOUND');

// 2. Add saveDraft JS function and beforeunload to the review page script
const oldScript = `'<script>let qLineCount='+nextLine+';'`;
const newScript = `'<script>' +${nl}      'let isDirty=false;' +${nl}      'document.querySelectorAll("input,textarea,select").forEach(function(el){el.addEventListener("change",function(){isDirty=true;});});' +${nl}      'window.addEventListener("beforeunload",function(e){if(isDirty){e.preventDefault();e.returnValue="";}});' +${nl}      'function saveDraft(){' +${nl}      '  isDirty=false;' +${nl}      '  const form=document.querySelector("form");' +${nl}      '  const data=new FormData(form);' +${nl}      '  data.append("draft","1");' +${nl}      '  fetch("/admin/rfqs/'+rfq.id+'/quote-draft",{method:"POST",body:data})' +${nl}      '  .then(function(r){return r.json();})' +${nl}      '  .then(function(d){' +${nl}      '    if(d.ok){' +${nl}      '      const btn=document.querySelector(".btn[onclick=\\'saveDraft()\\']");' +${nl}      '      if(btn){btn.textContent="Draft Saved ✓";btn.style.color="#4caf50";}' +${nl}      '    }' +${nl}      '  }).catch(function(){alert("Save failed");isDirty=true;});' +${nl}      '}' +${nl}      'let qLineCount='+nextLine+';'`;
if (a.includes(oldScript)) { a = a.replace(oldScript, newScript); console.log('Draft JS: ADDED'); }
else console.log('Draft JS: NOT FOUND');

// 3. Add draft route before the quote-review route
const insertBefore = "  router.post('/rfqs/:id/quote-review'";
const draftRoute = `  router.post('/rfqs/:id/quote-draft', async (req, res) => {${nl}    if (!requireAuth(req, res)) return;${nl}    try {${nl}      const pool = await getPool();${nl}      const rfqResult = await pool.request().input('id', sql.BigInt, req.params.id)${nl}        .query(\`SELECT h.*, c.first_name, c.last_name, c.email, c.company FROM rfq_headers h JOIN customers c ON c.id=h.customer_id WHERE h.id=@id\`);${nl}      if (!rfqResult.recordset.length) return res.json({ ok: false, error: 'RFQ not found' });${nl}      const rfq = rfqResult.recordset[0];${nl}      const { payment_terms, valid_days, notes, personal_message } = req.body;${nl}      const linesRaw = req.body.lines || {};${nl}      const linesArr = Object.values(linesRaw);${nl}      let subtotal = 0;${nl}      const processedLines = linesArr.map(function(l, i) {${nl}        const unitPrice = parseFloat(l.unit_price) || 0;${nl}        const unitCost = parseFloat(l.unit_cost) || 0;${nl}        const qty = parseInt(l.quantity) || 1;${nl}        const lineTotal = unitPrice * qty;${nl}        const lineCost = unitCost * qty;${nl}        subtotal += lineTotal;${nl}        const fulfillPart = (l.fulfillment_part || '').trim().toUpperCase();${nl}        const isNSN = /^\\d{4}-\\d{2}-\\d{3}-\\d{4}$/.test(fulfillPart);${nl}        return {${nl}          ...l,${nl}          line_number: i + 1,${nl}          nsn: isNSN ? fulfillPart : (l.original_nsn || null),${nl}          part_number: !isNSN && fulfillPart ? fulfillPart : (l.original_part || null),${nl}          item_name: l.item_name || null,${nl}          unit_price: unitPrice,${nl}          unit_cost: unitCost,${nl}          quantity: qty,${nl}          line_total: lineTotal,${nl}          line_cost: lineCost,${nl}          line_margin: lineTotal - lineCost,${nl}          markup_pct: unitCost > 0 ? ((unitPrice - unitCost) / unitCost) * 100 : 0,${nl}          margin_pct: lineTotal > 0 ? ((lineTotal - lineCost) / lineTotal) * 100 : 0,${nl}        };${nl}      });${nl}      const totalCost = processedLines.reduce(function(s, l) { return s + l.line_cost; }, 0);${nl}      const validUntil = new Date(Date.now() + parseInt(valid_days || 30) * 24 * 60 * 60 * 1000);${nl}      const quoteNumber = rfq.rfq_number.replace(/^RFQ-/, 'QT-');${nl}      // Check if draft exists for this RFQ${nl}      const existing = await pool.request().input('rfqId', sql.BigInt, rfq.id)${nl}        .query("SELECT id FROM quotes WHERE rfq_id=@rfqId AND status='Draft'");${nl}      if (existing.recordset.length) {${nl}        // Update existing draft${nl}        await pool.request()${nl}          .input('rfqId', sql.BigInt, rfq.id)${nl}          .input('subtotal', sql.Decimal(12,2), subtotal)${nl}          .input('totalAmount', sql.Decimal(12,2), subtotal)${nl}          .input('totalCost', sql.Decimal(12,2), totalCost)${nl}          .input('totalMargin', sql.Decimal(12,2), subtotal - totalCost)${nl}          .input('validUntil', sql.Date, validUntil)${nl}          .input('paymentTerms', sql.NVarChar(100), payment_terms || 'Credit Card or Wire Transfer')${nl}          .input('notes', sql.NVarChar(sql.MAX), notes || null)${nl}          .input('personalMessage', sql.NVarChar(sql.MAX), personal_message || null)${nl}          .query(\`UPDATE quotes SET subtotal=@subtotal,total_amount=@totalAmount,total_cost=@totalCost,total_margin=@totalMargin,valid_until=@validUntil,payment_terms=@paymentTerms,notes=@notes,personal_message=@personalMessage,updated_at=GETDATE() WHERE rfq_id=@rfqId AND status='Draft'\`);${nl}        // Delete old draft lines and reinsert${nl}        await pool.request().input('qid', sql.BigInt, existing.recordset[0].id)${nl}          .query('DELETE FROM quote_lines WHERE quote_id=@qid');${nl}        for (const l of processedLines) {${nl}          await pool.request()${nl}            .input('quoteId', sql.BigInt, existing.recordset[0].id)${nl}            .input('lineNum', sql.Int, l.line_number)${nl}            .input('nsn', sql.NVarChar(20), l.nsn || null)${nl}            .input('partNum', sql.NVarChar(100), l.part_number || null)${nl}            .input('itemName', sql.NVarChar(255), l.item_name || null)${nl}            .input('condition', sql.NVarChar(5), l.condition_code || 'NE')${nl}            .input('qty', sql.Int, l.quantity)${nl}            .input('unitCost', sql.Decimal(10,2), l.unit_cost)${nl}            .input('unitPrice', sql.Decimal(10,2), l.unit_price)${nl}            .input('lineTotal', sql.Decimal(12,2), l.line_total)${nl}            .input('lineCost', sql.Decimal(12,2), l.line_cost)${nl}            .input('lineMargin', sql.Decimal(12,2), l.line_margin)${nl}            .input('markupPct', sql.Decimal(5,2), l.markup_pct)${nl}            .input('marginPct', sql.Decimal(5,2), l.margin_pct)${nl}            .input('rfqLineId', sql.BigInt, parseInt(l.rfq_line_id) || null)${nl}            .input('leadTime', sql.Int, parseInt((l.lead_time_days||'').toString().replace(/[^0-9]/,'')) || null)${nl}            .query(\`INSERT INTO quote_lines (quote_id,rfq_line_id,line_number,nsn,part_number,item_name,condition_code,quantity,unit_cost,unit_price,line_total,line_cost,line_margin,markup_pct,margin_pct,lead_time_days) VALUES (@quoteId,@rfqLineId,@lineNum,@nsn,@partNum,@itemName,@condition,@qty,@unitCost,@unitPrice,@lineTotal,@lineCost,@lineMargin,@markupPct,@marginPct,@leadTime)\`);${nl}        }${nl}        return res.json({ ok: true, message: 'Draft updated' });${nl}      } else {${nl}        // Create new draft${nl}        const qr = await pool.request()${nl}          .input('rfqId', sql.BigInt, rfq.id)${nl}          .input('customerId', sql.BigInt, rfq.customer_id)${nl}          .input('quoteNumber', sql.NVarChar(20), quoteNumber)${nl}          .input('subtotal', sql.Decimal(12,2), subtotal)${nl}          .input('totalAmount', sql.Decimal(12,2), subtotal)${nl}          .input('totalCost', sql.Decimal(12,2), totalCost)${nl}          .input('totalMargin', sql.Decimal(12,2), subtotal - totalCost)${nl}          .input('validUntil', sql.Date, validUntil)${nl}          .input('paymentTerms', sql.NVarChar(100), payment_terms || 'Credit Card or Wire Transfer')${nl}          .input('notes', sql.NVarChar(sql.MAX), notes || null)${nl}          .input('personalMessage', sql.NVarChar(sql.MAX), personal_message || null)${nl}          .query(\`INSERT INTO quotes (rfq_id,customer_id,quote_number,subtotal,total_amount,total_cost,total_margin,valid_until,payment_terms,notes,personal_message,status) OUTPUT INSERTED.id VALUES (@rfqId,@customerId,@quoteNumber,@subtotal,@totalAmount,@totalCost,@totalMargin,@validUntil,@paymentTerms,@notes,@personalMessage,'Draft')\`);${nl}        const quoteId = qr.recordset[0].id;${nl}        for (const l of processedLines) {${nl}          await pool.request()${nl}            .input('quoteId', sql.BigInt, quoteId)${nl}            .input('lineNum', sql.Int, l.line_number)${nl}            .input('nsn', sql.NVarChar(20), l.nsn || null)${nl}            .input('partNum', sql.NVarChar(100), l.part_number || null)${nl}            .input('itemName', sql.NVarChar(255), l.item_name || null)${nl}            .input('condition', sql.NVarChar(5), l.condition_code || 'NE')${nl}            .input('qty', sql.Int, l.quantity)${nl}            .input('unitCost', sql.Decimal(10,2), l.unit_cost)${nl}            .input('unitPrice', sql.Decimal(10,2), l.unit_price)${nl}            .input('lineTotal', sql.Decimal(12,2), l.line_total)${nl}            .input('lineCost', sql.Decimal(12,2), l.line_cost)${nl}            .input('lineMargin', sql.Decimal(12,2), l.line_margin)${nl}            .input('markupPct', sql.Decimal(5,2), l.markup_pct)${nl}            .input('marginPct', sql.Decimal(5,2), l.margin_pct)${nl}            .input('rfqLineId', sql.BigInt, parseInt(l.rfq_line_id) || null)${nl}            .input('leadTime', sql.Int, parseInt((l.lead_time_days||'').toString().replace(/[^0-9]/,'')) || null)${nl}            .query(\`INSERT INTO quote_lines (quote_id,rfq_line_id,line_number,nsn,part_number,item_name,condition_code,quantity,unit_cost,unit_price,line_total,line_cost,line_margin,markup_pct,margin_pct,lead_time_days) VALUES (@quoteId,@rfqLineId,@lineNum,@nsn,@partNum,@itemName,@condition,@qty,@unitCost,@unitPrice,@lineTotal,@lineCost,@lineMargin,@markupPct,@marginPct,@leadTime)\`);${nl}        }${nl}        return res.json({ ok: true, message: 'Draft saved' });${nl}      }${nl}    } catch(err) {${nl}      console.error('Draft save error:', err);${nl}      res.json({ ok: false, error: err.message });${nl}    }${nl}  });${nl}${nl}  `;

if (a.includes(insertBefore)) {
  a = a.replace(insertBefore, draftRoute + insertBefore);
  console.log('Draft route: ADDED');
} else console.log('Draft route: NOT FOUND');

// 4. Add "Resume Draft" check to the RFQ detail page inline form
// When + New Quote is clicked, check for existing draft
const oldToggle = `<button class="btn btn-gold btn-sm" onclick="var f=document.getElementById('quote-form');f.style.display=f.style.display==='none'?'block':'none';">+ New Quote</button>`;
const newToggle = `<button class="btn btn-gold btn-sm" onclick="var f=document.getElementById('quote-form');f.style.display=f.style.display==='none'?'block':'none';">+ New Quote</button>\${existingDraft ? ' <a href=\\"/admin/rfqs/'+rfq.id+'/quote-review-draft\\" class=\\"btn btn-outline btn-sm\\" style=\\"border-color:#4caf50;color:#4caf50;\\">Resume Draft</a>' : ''}`;

// Actually let's do this differently - check for draft in the route and add a banner
// Find where we build the RFQ detail page and add draft check
const oldSuccess = `      const successMsg = req.query.created ? '<div class="alert alert-success">Manual RFQ created successfully!</div>'`;
const newSuccess = `      // Check for existing draft quote
      const draftCheck = await pool.request().input('rfqIdDraft', sql.BigInt, req.params.id)
        .query("SELECT id FROM quotes WHERE rfq_id=@rfqIdDraft AND status='Draft'");
      const existingDraft = draftCheck.recordset.length > 0 ? draftCheck.recordset[0] : null;
      const successMsg = req.query.created ? '<div class="alert alert-success">Manual RFQ created successfully!</div>'`;

if (a.includes(oldSuccess)) { a = a.replace(oldSuccess, newSuccess); console.log('Draft check: ADDED'); }
else console.log('Draft check: NOT FOUND');

// Add Resume Draft button next to + New Quote button
const oldNewQuote = `<button class="btn btn-gold btn-sm" onclick="var f=document.getElementById('quote-form');f.style.display=f.style.display==='none'?'block':'none';">+ New Quote</button>`;
const newNewQuote = `<button class="btn btn-gold btn-sm" onclick="var f=document.getElementById('quote-form');f.style.display=f.style.display==='none'?'block':'none';">+ New Quote</button>\${existingDraft ? \`<a href="/admin/rfqs/\${rfq.id}/quote-review-draft" class="btn btn-outline btn-sm" style="border-color:#4caf50;color:#4caf50;margin-left:8px;">Resume Draft</a>\` : ''}`;
if (a.includes(oldNewQuote)) { a = a.replace(oldNewQuote, newNewQuote); console.log('Resume Draft btn: ADDED'); }
else console.log('Resume Draft btn: NOT FOUND');

fs.writeFileSync('admin/index.js', a);
console.log('All done!');
