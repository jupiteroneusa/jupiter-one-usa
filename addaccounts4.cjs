const fs = require('fs');
let a = fs.readFileSync('admin/index.js', 'utf8');

// Handle both Windows \r\n and Unix \n line endings
const insertBefore = "  // Customers List\r\n  router.get('/customers'";
const insertBeforeUnix = "  // Customers List\n  router.get('/customers'";

const found = a.includes(insertBefore) ? insertBefore : (a.includes(insertBeforeUnix) ? insertBeforeUnix : null);
const nl = a.includes('\r\n') ? '\r\n' : '\n';

if (!found) {
  console.log('NOT FOUND');
} else {
  const accountsCode = `  // Accounts${nl}  router.get('/accounts', async (req, res) => {${nl}    if (!requireAuth(req, res)) return;${nl}    const search = (req.query.q || '').trim();${nl}    const type = req.query.type || 'company';${nl}    try {${nl}      const pool = await getPool();${nl}      let rows = '', headers = '';${nl}      if (search) {${nl}        if (type === 'ref') {${nl}          headers = '<th>Ref</th><th>Customer</th><th>Company</th><th>RFQs</th><th>Quotes</th><th>Total Quoted</th><th>Last Activity</th>';${nl}          const result = await pool.request().input('q', sql.NVarChar, '%'+search+'%')${nl}            .query(\`SELECT h.customer_ref, COUNT(DISTINCT h.id) AS rfq_count, COUNT(DISTINCT q.id) AS quote_count,${nl}              SUM(ISNULL(q.total_amount,0)) AS total_quoted, MAX(h.submitted_at) AS last_activity,${nl}              MAX(c.first_name+' '+c.last_name) AS customer_name, MAX(c.company) AS company${nl}              FROM rfq_headers h JOIN customers c ON c.id=h.customer_id LEFT JOIN quotes q ON q.rfq_id=h.id${nl}              WHERE h.customer_ref LIKE @q GROUP BY h.customer_ref ORDER BY last_activity DESC\`);${nl}          rows = result.recordset.map(function(r){${nl}            return '<tr><td><a href="/admin/accounts/ref/'+encodeURIComponent(r.customer_ref)+'" style="color:#c8932a;font-family:monospace;">'+r.customer_ref+'</a></td>'+${nl}              '<td style="color:#7a8a9a;">'+(r.customer_name||'')+'</td><td style="color:#7a8a9a;">'+(r.company||'--')+'</td>'+${nl}              '<td>'+r.rfq_count+'</td><td>'+r.quote_count+'</td>'+${nl}              '<td style="font-weight:600;">$'+parseFloat(r.total_quoted||0).toLocaleString('en-US',{minimumFractionDigits:2})+'</td>'+${nl}              '<td style="color:#7a8a9a;font-size:.78rem;">'+new Date(r.last_activity).toLocaleDateString()+'</td></tr>';${nl}          }).join('') || '<tr><td colspan="7" style="text-align:center;color:#7a8a9a;padding:24px;">No results</td></tr>';${nl}        } else {${nl}          headers = '<th>Company</th><th>Contacts</th><th>RFQs</th><th>Quotes</th><th>Total Quoted</th><th>Last Activity</th>';${nl}          const result = await pool.request().input('q', sql.NVarChar, '%'+search+'%')${nl}            .query(\`SELECT c.company, COUNT(DISTINCT c.id) AS contact_count, COUNT(DISTINCT h.id) AS rfq_count,${nl}              COUNT(DISTINCT q.id) AS quote_count, SUM(ISNULL(q.total_amount,0)) AS total_quoted, MAX(h.submitted_at) AS last_activity${nl}              FROM customers c LEFT JOIN rfq_headers h ON h.customer_id=c.id LEFT JOIN quotes q ON q.rfq_id=h.id${nl}              WHERE c.company LIKE @q GROUP BY c.company ORDER BY last_activity DESC\`);${nl}          rows = result.recordset.map(function(r){${nl}            return '<tr><td><a href="/admin/accounts/company/'+encodeURIComponent(r.company)+'" style="color:#c8932a;">'+(r.company||'')+'</a></td>'+${nl}              '<td>'+r.contact_count+'</td><td>'+r.rfq_count+'</td><td>'+r.quote_count+'</td>'+${nl}              '<td style="font-weight:600;">$'+parseFloat(r.total_quoted||0).toLocaleString('en-US',{minimumFractionDigits:2})+'</td>'+${nl}              '<td style="color:#7a8a9a;font-size:.78rem;">'+(r.last_activity?new Date(r.last_activity).toLocaleDateString():'--')+'</td></tr>';${nl}          }).join('') || '<tr><td colspan="6" style="text-align:center;color:#7a8a9a;padding:24px;">No results</td></tr>';${nl}        }${nl}      }${nl}      const isRef = type === 'ref';${nl}      const tableHtml = search${nl}        ? ('<div class="card"><div class="card-header">Results for "'+search+'"</div><div style="overflow-x:auto;"><table><thead><tr>'+headers+'</tr></thead><tbody>'+rows+'</tbody></table></div></div>')${nl}        : '<div style="text-align:center;padding:60px;color:#7a8a9a;"><div style="font-size:2rem;margin-bottom:12px;">&#128194;</div><div>Search by company or customer reference</div></div>';${nl}      res.send(page('Accounts','accounts', SORT_SCRIPT+${nl}        '<div class="page-title">Accounts</div><div class="page-sub">Search by company or customer reference</div>'+${nl}        '<div class="card" style="margin-bottom:20px;"><div class="card-body">'+${nl}        '<form method="GET" action="/admin/accounts" style="display:flex;gap:10px;flex-wrap:wrap;align-items:flex-end;">'+${nl}        '<div style="flex:1;min-width:250px;"><div style="font-size:.7rem;color:#7a8a9a;margin-bottom:4px;">Search</div>'+${nl}        '<input type="text" name="q" value="'+search+'" placeholder="'+(isRef?'Customer ref...':'Company name...')+'" style="width:100%;" autofocus/></div>'+${nl}        '<div><div style="font-size:.7rem;color:#7a8a9a;margin-bottom:4px;">Search By</div>'+${nl}        '<select name="type" style="width:150px;"><option value="company"'+(!isRef?' selected':'')+'>Company</option>'+${nl}        '<option value="ref"'+(isRef?' selected':'')+'>Customer Ref</option></select></div>'+${nl}        '<button type="submit" class="btn btn-gold">Search &rarr;</button>'+${nl}        (search?' <a href="/admin/accounts" class="btn btn-outline">Clear</a>':'')+${nl}        '</form></div></div>'+tableHtml));${nl}    } catch(err) {${nl}      res.send(page('Accounts','accounts','<div class="alert alert-error">'+err.message+'</div>'));${nl}    }${nl}  });${nl}${nl}  router.get('/accounts/ref/:ref', async (req, res) => {${nl}    if (!requireAuth(req, res)) return;${nl}    const ref = decodeURIComponent(req.params.ref);${nl}    try {${nl}      const pool = await getPool();${nl}      const rfqs = await pool.request().input('ref', sql.NVarChar, ref).query(\`${nl}        SELECT h.id, h.rfq_number, h.status, h.priority, h.submitted_at,${nl}          c.id AS customer_id, c.first_name+' '+c.last_name AS customer_name, c.company,${nl}          COUNT(l.id) AS line_count, q.id AS quote_id, q.quote_number, q.total_amount, q.status AS quote_status${nl}        FROM rfq_headers h JOIN customers c ON c.id=h.customer_id${nl}        LEFT JOIN rfq_lines l ON l.rfq_id=h.id LEFT JOIN quotes q ON q.rfq_id=h.id${nl}        WHERE h.customer_ref=@ref${nl}        GROUP BY h.id,h.rfq_number,h.status,h.priority,h.submitted_at,c.id,c.first_name,c.last_name,c.company,q.id,q.quote_number,q.total_amount,q.status${nl}        ORDER BY h.submitted_at DESC\`);${nl}      const total = rfqs.recordset.reduce(function(s,r){return s+parseFloat(r.total_amount||0);},0);${nl}      const rfqRows = rfqs.recordset.map(function(r){${nl}        return '<tr><td class="mono text-gold"><a href="/admin/rfqs/'+r.id+'" style="color:#c8932a;">'+r.rfq_number+'</a></td>'+${nl}          '<td><a href="/admin/customers/'+r.customer_id+'" style="color:#c8932a;">'+r.customer_name+'</a><br><span style="font-size:.75rem;color:#7a8a9a;">'+(r.company||'')+'</span></td>'+${nl}          '<td>'+r.line_count+'</td><td>'+statusBadge(r.priority)+'</td><td>'+statusBadge(r.status)+'</td>'+${nl}          '<td>'+(r.quote_number?'<a href="/admin/quotes/'+r.quote_id+'" style="color:#c8932a;">'+r.quote_number+'</a>':'--')+'</td>'+${nl}          '<td>'+(r.total_amount?'$'+parseFloat(r.total_amount).toLocaleString('en-US',{minimumFractionDigits:2}):'--')+'</td>'+${nl}          '<td>'+(r.quote_status?statusBadge(r.quote_status):'--')+'</td>'+${nl}          '<td style="color:#7a8a9a;font-size:.78rem;">'+new Date(r.submitted_at).toLocaleDateString()+'</td></tr>';${nl}      }).join('') || '<tr><td colspan="9" style="text-align:center;color:#7a8a9a;padding:24px;">No RFQs</td></tr>';${nl}      res.send(page('Ref: '+ref,'accounts',${nl}        '<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:6px;">'+${nl}        '<div class="page-title">Ref: <span style="color:#c8932a;font-family:monospace;">'+ref+'</span></div>'+${nl}        '<a href="/admin/accounts?type=ref&q='+encodeURIComponent(ref)+'" class="btn btn-outline btn-sm">&larr; Back</a></div>'+${nl}        '<div class="stat-grid" style="margin-bottom:20px;">'+${nl}        '<div class="stat"><div class="stat-num">'+rfqs.recordset.length+'</div><div class="stat-label">RFQs</div></div>'+${nl}        '<div class="stat"><div class="stat-num">'+rfqs.recordset.filter(function(r){return r.quote_id;}).length+'</div><div class="stat-label">Quotes</div></div>'+${nl}        '<div class="stat"><div class="stat-num">$'+total.toLocaleString('en-US',{minimumFractionDigits:2})+'</div><div class="stat-label">Total Quoted</div></div></div>'+${nl}        '<div class="card"><div class="card-header">RFQs & Quotes</div><div style="overflow-x:auto;">'+${nl}        '<table><thead><tr><th>RFQ #</th><th>Customer</th><th>Lines</th><th>Priority</th><th>RFQ Status</th><th>Quote #</th><th>Amount</th><th>Quote Status</th><th>Date</th></tr></thead>'+${nl}        '<tbody>'+rfqRows+'</tbody></table></div></div>'));${nl}    } catch(err) {${nl}      res.send(page('Account','accounts','<div class="alert alert-error">'+err.message+'</div>'));${nl}    }${nl}  });${nl}${nl}  router.get('/accounts/company/:company', async (req, res) => {${nl}    if (!requireAuth(req, res)) return;${nl}    const company = decodeURIComponent(req.params.company);${nl}    try {${nl}      const pool = await getPool();${nl}      const contacts = await pool.request().input('co', sql.NVarChar, company)${nl}        .query(\`SELECT id, first_name+' '+last_name AS name, email, phone, status, created_at FROM customers WHERE company=@co ORDER BY last_name\`);${nl}      const rfqs = await pool.request().input('co2', sql.NVarChar, company).query(\`${nl}        SELECT h.id, h.rfq_number, h.status, h.priority, h.customer_ref, h.submitted_at,${nl}          c.id AS customer_id, c.first_name+' '+c.last_name AS customer_name, COUNT(l.id) AS line_count,${nl}          q.id AS quote_id, q.quote_number, q.total_amount, q.status AS quote_status${nl}        FROM rfq_headers h JOIN customers c ON c.id=h.customer_id${nl}        LEFT JOIN rfq_lines l ON l.rfq_id=h.id LEFT JOIN quotes q ON q.rfq_id=h.id${nl}        WHERE c.company=@co2${nl}        GROUP BY h.id,h.rfq_number,h.status,h.priority,h.customer_ref,h.submitted_at,c.id,c.first_name,c.last_name,q.id,q.quote_number,q.total_amount,q.status${nl}        ORDER BY h.submitted_at DESC\`);${nl}      const total = rfqs.recordset.reduce(function(s,r){return s+parseFloat(r.total_amount||0);},0);${nl}      const contactRows = contacts.recordset.map(function(c){${nl}        return '<tr><td><a href="/admin/customers/'+c.id+'" style="color:#c8932a;">'+c.name+'</a></td>'+${nl}          '<td style="color:#7a8a9a;font-size:.8rem;">'+c.email+'</td><td style="color:#7a8a9a;">'+(c.phone||'--')+'</td>'+${nl}          '<td>'+statusBadge(c.status)+'</td><td style="color:#7a8a9a;font-size:.78rem;">'+new Date(c.created_at).toLocaleDateString()+'</td></tr>';${nl}      }).join('');${nl}      const rfqRows = rfqs.recordset.map(function(r){${nl}        return '<tr><td class="mono text-gold"><a href="/admin/rfqs/'+r.id+'" style="color:#c8932a;">'+r.rfq_number+'</a></td>'+${nl}          '<td><a href="/admin/customers/'+r.customer_id+'" style="color:#c8932a;">'+r.customer_name+'</a></td>'+${nl}          '<td>'+(r.customer_ref?'<a href="/admin/accounts/ref/'+encodeURIComponent(r.customer_ref)+'" style="color:#c8932a;font-family:monospace;">'+r.customer_ref+'</a>':'--')+'</td>'+${nl}          '<td>'+r.line_count+'</td><td>'+statusBadge(r.priority)+'</td><td>'+statusBadge(r.status)+'</td>'+${nl}          '<td>'+(r.quote_number?'<a href="/admin/quotes/'+r.quote_id+'" style="color:#c8932a;">'+r.quote_number+'</a>':'--')+'</td>'+${nl}          '<td>'+(r.total_amount?'$'+parseFloat(r.total_amount).toLocaleString('en-US',{minimumFractionDigits:2}):'--')+'</td>'+${nl}          '<td style="color:#7a8a9a;font-size:.78rem;">'+new Date(r.submitted_at).toLocaleDateString()+'</td></tr>';${nl}      }).join('') || '<tr><td colspan="9" style="text-align:center;color:#7a8a9a;padding:24px;">No RFQs yet</td></tr>';${nl}      res.send(page('Company: '+company,'accounts',${nl}        '<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:6px;">'+${nl}        '<div class="page-title">'+company+'</div>'+${nl}        '<a href="/admin/accounts?type=company&q='+encodeURIComponent(company)+'" class="btn btn-outline btn-sm">&larr; Back</a></div>'+${nl}        '<div class="stat-grid" style="margin-bottom:20px;">'+${nl}        '<div class="stat"><div class="stat-num">'+contacts.recordset.length+'</div><div class="stat-label">Contacts</div></div>'+${nl}        '<div class="stat"><div class="stat-num">'+rfqs.recordset.length+'</div><div class="stat-label">RFQs</div></div>'+${nl}        '<div class="stat"><div class="stat-num">'+rfqs.recordset.filter(function(r){return r.quote_id;}).length+'</div><div class="stat-label">Quotes</div></div>'+${nl}        '<div class="stat"><div class="stat-num">$'+total.toLocaleString('en-US',{minimumFractionDigits:2})+'</div><div class="stat-label">Total Quoted</div></div></div>'+${nl}        '<div class="card" style="margin-bottom:20px;"><div class="card-header">Contacts</div>'+${nl}        '<table><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Status</th><th>Joined</th></tr></thead><tbody>'+contactRows+'</tbody></table></div>'+${nl}        '<div class="card"><div class="card-header">RFQs & Quotes</div><div style="overflow-x:auto;">'+${nl}        '<table><thead><tr><th>RFQ #</th><th>Contact</th><th>Cust Ref</th><th>Lines</th><th>Priority</th><th>RFQ Status</th><th>Quote #</th><th>Amount</th><th>Date</th></tr></thead>'+${nl}        '<tbody>'+rfqRows+'</tbody></table></div></div>'));${nl}    } catch(err) {${nl}      res.send(page('Account','accounts','<div class="alert alert-error">'+err.message+'</div>'));${nl}    }${nl}  });${nl}${nl}  // Customers List${nl}  router.get('/customers'`;

  a = a.replace(found, accountsCode);
  console.log('Accounts routes: ADDED');
}

fs.writeFileSync('admin/index.js', a);
console.log('Done.');
